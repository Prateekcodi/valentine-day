# Server Environment Variables
PORT=3001
CORS_ORIGIN=http://localhost:3000

# Optional: MongoDB (for production persistence)
MONGODB_URI=mongodb+srv://<username>:<password>@cluster.mongodb.net/valentine-week

# Optional: Gemini API (for AI reflections)
# If not provided, fallback reflections will be used
GEMINI_API_KEY=your_gemini_api_key_here
