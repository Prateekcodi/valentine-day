# Server Environment Variables
PORT=3001
CORS_ORIGIN=http://localhost:3000

# Optional: MongoDB (for production persistence)
MONGODB_URI=mongodb+srv://<username>:<password>@cluster.mongodb.net/valentine-week

# MiniMax API (primary AI - get from https://www.minimaxi.com/)
MINIMAX_API_KEY=your_minimax_api_key_here

# Optional: Gemini API (fallback AI - get from https://ai.google.dev/)
# If MiniMax is provided, Gemini will only be used if MiniMax fails
GEMINI_API_KEY=your_gemini_api_key_here
